/// <reference types="vitest" />
/// <reference types="node" />

// Este arquivo expõe as tipagens globais do Vitest (describe, it, expect, beforeEach, afterAll, etc.)
// para os arquivos .test.ts dentro de src/. Mantém o editor sem erros de tipo.
