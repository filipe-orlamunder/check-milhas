/** @type {import('tailwindcss').Config} */
export default {
  // Define os arquivos onde o Tailwind deve procurar por classes
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  // Configurações do tema, onde novas cores, fontes, etc., podem ser estendidas
  theme: {
    extend: {},
  },
  // Lista de plugins do Tailwind CSS a serem utilizados
  plugins: [],
};
